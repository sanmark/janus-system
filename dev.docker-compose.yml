version: '3'
services:
    lk-sanmark-janus:
        build:
            context: .
            dockerfile: dev.Dockerfile
        image: lk-sanmark-janus-dev
        ports:
        - 80:80
        volumes:
        - ./src:/app
        env_file:
        - ./src/.env
